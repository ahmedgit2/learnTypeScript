import {StyleSheet} from 'react-native';
import {appColor} from '../../utils';

export const styles = StyleSheet.create({
  containerStyle: {flexGrow: 1, justifyContent: 'center', alignItems: 'center'},
  textStyle: {
    fontSize: 16,
    color: appColor.mainText,
  },
});
