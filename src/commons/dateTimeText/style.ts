import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerStyle: {
    flexDirection: 'row',
    margin: 5,
  },
  textStyle: {
    marginHorizontal: 3,
    color: '#999',
    fontSize: 14,
  },
});
